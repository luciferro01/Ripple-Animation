import 'package:flutter/material.dart';

class AnimationTimePass extends StatelessWidget {
  const AnimationTimePass({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('MOHIL BANSAL'),
      ),
    );
  }
}
